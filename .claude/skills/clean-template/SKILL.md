---
name: clean-template
description: (統合済み) /init-mcp-server に統合されたため、そちらを案内する
user-invocable: true
---

# テンプレートクリーンアップスキル（統合済み）

このスキルは `/init-mcp-server` に統合されました。

## 手順

1. ユーザーに以下を案内して終了する:
   - テンプレートのクリーンアップは `/init-mcp-server` の Phase 2 で自動実行される
   - 新規プロジェクトの初期化には `/init-mcp-server <server-name>` を使用すること
